import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    token: ""
};

export const currentTokenSlice = createSlice({
    name: 'currentToken',
    initialState,
    reducers: {
        
    }
})